PS11

Position class:

	new_pos(agle,speed)


Room class:

	clean(pos)
	is_clean(m,n)
	total_tiles()
	num_clean_tiles()
	get_coverage()
	random_pos()
	pos_exist(pos)

robot class:

	get_pos()
	get_direction()
	set_pos(pos)
	set_direction()